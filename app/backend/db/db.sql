CREATE TABLE IF NOT EXISTS categories (id INTEGER PRIMARY KEY NOT NULL, name TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS items (id INTEGER PRIMARY KEY NOT NULL, categoryId INTEGER, name TEXT NOT NULL, FOREIGN KEY(categoryId) REFERENCES categories(id));